management:
  endpoints:
    web:
      exposure:
        include: '*'
spring:
  application:
    name: eureka-service
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: native
